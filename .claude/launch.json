{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "pennylane-dashboard",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "cd /Users/jeromedx6/Desktop/pennylane-dashboard && export $(grep -v '^#' .env | xargs) && python3 -m uvicorn api.main:app --port $PORT --host 0.0.0.0"],
      "port": 8080
    }
  ]
}
